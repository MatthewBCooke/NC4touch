# Virtual Chamber Requirements
# Minimal dependencies for running virtual chamber (no hardware needed)

# Core dependencies
pyyaml>=5.1  # For configuration files

# GUI dependencies (optional - only needed if using VirtualChamberGUI)
# tkinter - Usually included with Python, but if not:
# - Ubuntu/Debian: sudo apt-get install python3-tk
# - Fedora: sudo dnf install python3-tkinter  
# - macOS: Included with Python from python.org
# - Windows: Included with Python installer

# Note: Virtual chamber has NO hardware dependencies!
# No pigpio, no serial, no GPIO required.
